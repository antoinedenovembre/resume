name: Compile resume

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Compile resume with image (English)
        uses: xu-cheng/latex-action@v3
        with:
          root_file: resume_with_image_en.tex
          working_directory: build
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode

      - name: Compile resume with image (French)
        uses: xu-cheng/latex-action@v3
        with:
          root_file: resume_with_image_fr.tex
          working_directory: build
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode

      - name: Compile resume without image (English)
        uses: xu-cheng/latex-action@v3
        with:
          root_file: resume_no_image_en.tex
          working_directory: build
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode

      - name: Compile resume without image (French)
        uses: xu-cheng/latex-action@v3
        with:
          root_file: resume_no_image_fr.tex
          working_directory: build
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode

      - name: Upload PDF artifacts
        uses: actions/upload-artifact@v4
        with:
          name: resume-pdfs
          path: |
            build/resume_with_image_en.pdf
            build/resume_no_image_en.pdf
            build/resume_with_image_fr.pdf
            build/resume_no_image_fr.pdf
          retention-days: 30

      - name: Delete existing latest release
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        continue-on-error: true
        run: |
          gh release delete latest --yes || true
          git push --delete origin latest || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare files for release
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          # Copy and rename PDFs to avoid name conflicts
          cp build/resume_with_image_en.pdf resume-with-image-en.pdf
          cp build/resume_no_image_en.pdf resume-no-image-en.pdf
          cp build/resume_with_image_fr.pdf resume-with-image-fr.pdf
          cp build/resume_no_image_fr.pdf resume-no-image-fr.pdf
          ls -la *.pdf

      - name: Create Latest Release
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest resume"
          body: |
            Latest compiled resume PDFs from commit ${{ github.sha }}

            **Files included:**
            - `resume-with-image-en.pdf` - Resume with headshot (English)
            - `resume-no-image-en.pdf` - Resume without headshot (English)
            - `resume-with-image-fr.pdf` - Resume with headshot (French)
            - `resume-no-image-fr.pdf` - Resume without headshot (French)

            **Build date:** ${{ github.event.head_commit.timestamp }}
          files: resume-with-image-en.pdf,resume-no-image-en.pdf,resume-with-image-fr.pdf,resume-no-image-fr.pdf
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Tagged Release
        if: startsWith(github.ref, 'refs/tags/') && github.ref != 'refs/tags/latest'
        run: |
          # Copy and rename PDFs for tagged releases too
          cp build/resume_with_image_en.pdf resume-with-image-en.pdf
          cp build/resume_no_image_en.pdf resume-no-image-en.pdf
          cp build/resume_with_image_fr.pdf resume-with-image-fr.pdf
          cp build/resume_no_image_fr.pdf resume-no-image-fr.pdf

      - name: Upload Tagged Release
        if: startsWith(github.ref, 'refs/tags/') && github.ref != 'refs/tags/latest'
        uses: softprops/action-gh-release@v2
        with:
          files: resume-with-image-en.pdf,resume-no-image-en.pdf,resume-with-image-fr.pdf,resume-no-image-fr.pdf
          draft: false
          token: ${{ secrets.GITHUB_TOKEN }}
